package grakn.benchmark.metric;

import org.nd4j.linalg.api.ndarray.INDArray;
import org.nd4j.linalg.factory.Nd4j;

import java.util.LinkedList;
import java.util.List;

public class DegreeDistribution {

    public static double[] discreteDistribution(GraphProperties properties, double[] percentiles) {

//        INDArray percentilesArray = Nd4j.create(percentiles);

        // consume stream of Integers representing vertex degrees

//        LinkedList<Integer> allDegrees = new LinkedList<>();
//        properties.vertexDegree().forEach(degree -> allDegrees.add(degree));
//
//        INDArray degrees = Nd4j.create(allDegrees);

//        long numVertices = degrees.shape();
//        INDArray percentileIndices =


        return null;
    }
}
